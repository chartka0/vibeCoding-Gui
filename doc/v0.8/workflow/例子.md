/ccg:workflow 你看看这个项目里的完整工作流应该要怎么优化

1.[模式：研究]

  已完成项目深度分析。现在进入 Prompt 增强 和 需求完整性评分。

  📊 需求增强分析

  原始需求："你看看这个项目里的完整工作流应该要怎么优化"

  增强后的结构化需求：

  目标：优化 vibeCoding 桌面应用的完整工作流实现，提升用户体验、系统稳定性和多模型协作效率

  技术约束：
  - 前端：Tauri + Vue 3 + Naive UI
  - 后端：Rust (tokio async)
  - 工作流引擎：CCG (Claude + Codex + Gemini)
  - 数据库：SQLite (workflow_runs 历史记录)

  范围边界：
  1. WorkflowPanel.vue 的 UI/UX 优化
  2. Rust 后端 workflow 调度器优化
  3. 日志流式传输和步骤追踪机制
  4. 错误处理和用户反馈改进
  5. 与 CCG 原生 6 阶段工作流的对齐

  验收标准：
  - 用户能清晰看到 6 个阶段的实时进度
  - 日志输出能准确反映当前执行状态
  - 支持中断、恢复、错误重试
  - 与 CCG 命令行工具行为一致

  📈 需求完整性评分

  - 目标明确性：2.5/3 - 优化方向明确，但缺少具体痛点
  - 预期结果：2/3 - 隐含了改进体验，但未量化指标
  - 边界范围：2/2 - 项目结构清晰，范围可控
  - 约束条件：2/2 - 技术栈固定，架构已定

  总分：8.5/10 ✅ 继续

  ---
  🔍 项目现状分析

  当前架构

  vibeCoding/
  ├── ccg-workflow/          # CCG 多模型协作系统 (npm 包)
  │   ├── templates/commands/workflow.md  # 6 阶段工作流定义
  │   └── bin/codeagent-wrapper          # Go 包装器
  ├── vibe-ccg-gui/          # Tauri 桌面应用
  │   ├── src/
  │   │   ├── components/panels/WorkflowPanel.vue  # 工作流 UI
  │   │   └── store/workspace.ts                   # 状态管理
  │   └── src-tauri/
  │       └── src/lib.rs     # Rust 后端 (workflow 调度)
  └── doc/                   # 产品需求文档

  核心发现

  ✅ 已实现：
  1. 6 阶段步骤条 UI (研究→构思→计划→执行→优化→评审)
  2. 4 种执行模式 (full/frontend/backend/codex-exec)
  3. Rust 异步进程调度 + 日志流式传输
  4. SQLite 历史记录存储
  5. 工作区隔离 (多项目支持)

  ⚠️ 待优化问题：

  1. 步骤追踪不精确 (WorkflowPanel.vue:42-50)
    - 当前仅基于日志行数粗略推测进度
    - 无法识别 CCG 输出的 [模式：X] 标签
    - 用户看不到真实的阶段切换
  2. 命令调用错误 (lib.rs:110-120)
    - 直接调用 ccg-workflow 命令不存在
    - 应该调用 claude-code CLI + /ccg:workflow 斜杠命令
    - 参数传递方式不符合 CCG 规范
  3. 日志解析缺失
    - 未解析 SESSION_ID 用于会话复用
    - 未捕获错误和警告级别
    - 缺少结构化日志存储
  4. 用户体验问题
    - 无法中断正在运行的工作流
    - 失败后无重试机制
    - 缺少预估时间和进度百分比
  5. Mock 模式不真实 (lib.rs:129-146)
    - 开发环境的 mock 无法测试真实流程
    - 应该调用真实的 CCG 命令

  继续进入 阶段 2：方案构思？